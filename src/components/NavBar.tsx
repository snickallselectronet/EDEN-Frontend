import { useState } from "react";

function TouchAndStepContent() {
  return (
    <div>
      <ul className="list-group active">
        {/* <li className="list-group-item" onClick={handleFieldToggle}>
          Voltage Traverse
        </li>
        <li className="list-group-item" onClick={handleFieldToggle}>
          EPR Contours
        </li>
        <li className="list-group-item" onClick={handleFieldToggle}>
          Touch and Step
        </li> */}
      </ul>
    </div>
  );
}

export default TouchAndStepContent;
